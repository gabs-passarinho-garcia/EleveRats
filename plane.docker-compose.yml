# Copyright (C) 2026 Gabriel Passarinho Garcia and EleveRats Team
# This program is free software: you can redistribute it and/or modify
# it under the terms of the GNU Affero General Public License as
# published by the Free Software Foundation, either version 3 of the
# License, or (at your option) any later version.
#
# This program is distributed in the hope that it will be useful,
# but WITHOUT ANY WARRANTY; without even the implied warranty of
# MERCHANTABILITY or FITNESS FOR A PARTICULAR PURPOSE.  See the
# GNU Affero General Public License for more details.
#
# You should have received a copy of the GNU Affero General Public License
# along with this program.  If not, see <https://www.gnu.org/licenses/>.
# Plane Community Edition - Sub-stack for EleveRats
# Purified version: No Caddy Proxy, S3 Public Routing, and Subdomain support

services:
  plane-web:
    image: artifacts.plane.so/makeplane/plane-frontend:stable
    restart: always
    deploy:
      resources:
        limits:
          cpus: "0.5"
          memory: 512M
        reservations:
          cpus: "0.1"
          memory: 128M
    depends_on:
      - plane-api
      - plane-worker
    networks:
      eleverats-net:
        aliases:
          - web

  plane-space:
    image: artifacts.plane.so/makeplane/plane-space:stable
    restart: always
    deploy:
      resources:
        limits:
          cpus: "0.5"
          memory: 512M
        reservations:
          cpus: "0.1"
          memory: 128M
    healthcheck:
      test:
        ["CMD-SHELL", "wget -q -O- http://127.0.0.1:3000/ >/dev/null || exit 1"]
      interval: 30s
      timeout: 10s
      retries: 3
    depends_on:
      - plane-api
      - plane-worker
      - plane-web
    networks:
      eleverats-net:
        aliases:
          - space

  plane-admin:
    image: artifacts.plane.so/makeplane/plane-admin:stable
    restart: always
    deploy:
      resources:
        limits:
          cpus: "0.5"
          memory: 512M
        reservations:
          cpus: "0.1"
          memory: 128M
    depends_on:
      - plane-api
      - plane-web
    networks:
      eleverats-net:
        aliases:
          - admin

  plane-live:
    image: artifacts.plane.so/makeplane/plane-live:stable
    restart: always
    environment:
      API_BASE_URL: http://plane-api:8000
      LIVE_SERVER_SECRET_KEY: ${PLANE_LIVE_SERVER_SECRET_KEY:-2FiJk1U2aiVPEQtzLehYGlTSnTnrs7LW}
      REDIS_HOST: redis
      REDIS_PORT: 6379
      REDIS_URL: redis://:${REDIS_PASSWORD}@redis:6379/2
    depends_on:
      - plane-api
      - plane-web
    networks:
      eleverats-net:
        aliases:
          - live

  plane-api:
    image: artifacts.plane.so/makeplane/plane-backend:stable
    command: ./bin/docker-entrypoint-api.sh
    restart: always
    deploy:
      resources:
        limits:
          cpus: "1.0"
          memory: 1G
        reservations:
          cpus: "0.2"
          memory: 256M
    environment:
      # --- URLs de Acesso Público ---
      WEB_URL: https://plane.gabspassarinhogarcia.uk
      CORS_ALLOWED_ORIGINS: https://plane.gabspassarinhogarcia.uk,https://admin-plane.gabspassarinhogarcia.uk,https://spaces-plane.gabspassarinhogarcia.uk

      # --- Configuração do MinIO (Via Túnel Cloudflare) ---
      USE_MINIO: 1
      AWS_S3_ENDPOINT_URL: https://s3-eleverats.gabspassarinhogarcia.uk
      AWS_S3_BUCKET_NAME: plane-uploads
      MINIO_ENDPOINT_SSL: 1
      AWS_ACCESS_KEY_ID: ${MINIO_USER}
      AWS_SECRET_ACCESS_KEY: ${MINIO_PASSWORD}

      # --- Infraestrutura Interna ---
      PGHOST: pgbouncer
      PGDATABASE: plane_db
      POSTGRES_USER: plane_user
      POSTGRES_PASSWORD: ${PLANE_DB_PASSWORD}
      POSTGRES_DB: plane_db
      DATABASE_URL: postgresql://plane_user:${PLANE_DB_PASSWORD}@pgbouncer/plane_db
      DISABLE_SERVER_SIDE_CURSORS: 1
      REDIS_HOST: redis
      REDIS_PORT: 6379
      REDIS_URL: redis://:${REDIS_PASSWORD}@redis:6379/2
      AMQP_URL: amqp://plane:${RABBITMQ_PASSWORD}@plane-mq:5672/plane
      SECRET_KEY: ${PLANE_SECRET_KEY}
      LIVE_SERVER_SECRET_KEY: ${PLANE_LIVE_SERVER_SECRET_KEY}
      GUNICORN_WORKERS: 2
      DEBUG: 0
    depends_on:
      - pgbouncer
      - redis
      - plane-mq
    networks:
      eleverats-net:
        aliases:
          - api

  plane-worker:
    image: artifacts.plane.so/makeplane/plane-backend:stable
    command: ./bin/docker-entrypoint-worker.sh
    restart: always
    deploy:
      resources:
        limits:
          cpus: "1.0"
          memory: 1G
        reservations:
          cpus: "0.2"
          memory: 256M
    environment:
      # Sincronizado com a API
      DATABASE_URL: postgresql://plane_user:${PLANE_DB_PASSWORD}@pgbouncer/plane_db
      DISABLE_SERVER_SIDE_CURSORS: 1
      REDIS_URL: redis://:${REDIS_PASSWORD}@redis:6379/2
      AMQP_URL: amqp://plane:${RABBITMQ_PASSWORD}@plane-mq:5672/plane
      AWS_ACCESS_KEY_ID: ${MINIO_USER}
      AWS_SECRET_ACCESS_KEY: ${MINIO_PASSWORD}
      AWS_S3_ENDPOINT_URL: https://s3-eleverats.gabspassarinhogarcia.uk
      AWS_S3_BUCKET_NAME: plane-uploads
      SECRET_KEY: ${PLANE_SECRET_KEY}
      GUNICORN_WORKERS: 2
    depends_on:
      - plane-api
      - pgbouncer
      - redis
      - plane-mq
    networks:
      - eleverats-net

  plane-beat-worker:
    image: artifacts.plane.so/makeplane/plane-backend:stable
    command: ./bin/docker-entrypoint-beat.sh
    restart: always
    deploy:
      resources:
        limits:
          cpus: "0.5"
          memory: 512M
        reservations:
          cpus: "0.1"
          memory: 128M
    environment:
      DATABASE_URL: postgresql://plane_user:${PLANE_DB_PASSWORD}@pgbouncer/plane_db
      DISABLE_SERVER_SIDE_CURSORS: 1
      REDIS_URL: redis://:${REDIS_PASSWORD}@redis:6379/2
      AMQP_URL: amqp://plane:${RABBITMQ_PASSWORD}@plane-mq:5672/plane
      SECRET_KEY: ${PLANE_SECRET_KEY}
      GUNICORN_WORKERS: 2
    depends_on:
      - plane-api
      - pgbouncer
      - redis
      - plane-mq
    networks:
      - eleverats-net

  plane-migrator:
    image: artifacts.plane.so/makeplane/plane-backend:stable
    command: ./bin/docker-entrypoint-migrator.sh
    environment:
      DATABASE_URL: postgresql://plane_user:${PLANE_DB_PASSWORD}@pgbouncer/plane_db
      DISABLE_SERVER_SIDE_CURSORS: 1
      REDIS_URL: redis://:${REDIS_PASSWORD}@redis:6379/2
    depends_on:
      - pgbouncer
      - redis
    networks:
      - eleverats-net

  plane-mq:
    image: rabbitmq:3.13-management-alpine
    restart: always
    deploy:
      resources:
        limits:
          cpus: "0.5"
          memory: 512M
        reservations:
          cpus: "0.1"
          memory: 256M
    environment:
      RABBITMQ_DEFAULT_USER: plane
      RABBITMQ_DEFAULT_PASS: ${RABBITMQ_PASSWORD}
      RABBITMQ_DEFAULT_VHOST: plane
    volumes:
      - rabbitmq_data:/var/lib/rabbitmq
    networks:
      - eleverats-net

  minio-init:
    image: minio/mc:latest
    depends_on:
      - minio
    entrypoint: >
      /bin/sh -c "
      until (/usr/bin/mc alias set local http://minio:9000 ${MINIO_USER} ${MINIO_PASSWORD}) do echo '...waiting for minio...' && sleep 1; done;
      /usr/bin/mc mb local/plane-uploads || true;
      /usr/bin/mc anonymous set public local/plane-uploads;
      exit 0;
      "
    networks:
      - eleverats-net
